# Generate a 32-byte (256-bit) random key and Base64 encode it:
# openssl rand -base64 32
AGENTVAULT_SYSTEM_KEY=YOUR_SYSTEM_KEY_HERE

# Generate another 32-byte random key and Base64 encode it:
# openssl rand -base64 32
AGENTVAULT_JWT_SECRET=YOUR_JWT_SECRET_HERE

# Generate a 12-character random password:
# openssl rand -base64 12
AGENTVAULT_DEV_PASSWORD=YOUR_DEV_PASSWORD_HERE
