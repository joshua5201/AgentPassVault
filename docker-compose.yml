services:
  mongodb:
    image: mongo:6.0
    container_name: agentvault-mongodb
    ports:
      - "127.0.0.1:${MONGODB_PORT_27017:-27017}:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: agentvault

volumes:
  mongodb_data:
