# Development Configuration

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:${MYSQL_PORT_3306}/agentvault_dev?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Secrets (Expected from Environment Variables)
# AGENTVAULT_JWT_SECRET must be set in the environment or .env file
# agentvault.jwt.secret=