spring.application.name=agentpassvault
spring.profiles.active=dev
server.port=8080

# JPA Configuration
spring.jpa.open-in-view=false

# UI Configuration (dummy values)
agentpassvault.ui.base-url=https://vault.local
agentpassvault.ui.fulfillment-path=/requests/
agentpassvault.jwt.expiration-minutes=60
agentpassvault.jwt.lease-expiration-minutes=60
agentpassvault.jwt.refresh-expiration-minutes=1440

# SpringDoc OpenAPI
springdoc.api-docs.path=/v3/api-docs
springdoc.api-docs.enabled=true
springdoc.writer-with-default-pretty-printer=true

spring.flyway.enabled=true

# Security defaults
# source .env to load the env before running
#agentpassvault.jwt.secret=
